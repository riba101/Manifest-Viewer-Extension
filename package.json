{
  "name": "manifest-viewer-extension",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "lint": "eslint \"app/**/*.js\" \"__tests__/**/*.js\" \"scripts/**/*.js\"",
    "lint:fix": "npm run lint -- --fix",
    "test": "jest",
    "build": "npm run lint && npm test && node scripts/build-extension.js",
    "build:firefox": "npm run lint && npm test && node scripts/build-extension-firefox.js"
  },
  "devDependencies": {
    "archiver": "^6.0.1",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^29.7.0",
    "jsdom": "^24.0.0",
    "prettier": "^3.3.3"
  }
}
